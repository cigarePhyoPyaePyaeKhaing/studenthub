<%@ page contentType="text/html;charset=UTF-8" language="java" %><%@ taglib prefix="c" uri="jakarta.tags.core" %>
<!doctype html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Deadlines | StudentHub</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><script src="${pageContext.request.contextPath}/assets/js/main.js" defer></script><link href="${pageContext.request.contextPath}/assets/css/main.css" rel="stylesheet"><link href="${pageContext.request.contextPath}/assets/css/dashboard.css" rel="stylesheet"></head><body class="dashboard-body"><header class="mobile-header d-lg-none"><a class="dashboard-brand" href="${pageContext.request.contextPath}/home"><span>S</span> StudentHub</a></header><div class="dashboard-shell deadlines-shell"><aside class="dashboard-sidebar d-none d-lg-flex"><jsp:include page="../partials/sidebar.jsp" /></aside><main class="feed-column"><section class="welcome-panel"><div><p class="eyebrow mb-2">Academic planning</p><h1>Deadlines</h1><p class="mb-0 text-secondary">Assignments, exams, and important academic due dates for your scope.</p></div><c:if test="${canCreateDeadline}"><a class="btn btn-primary" href="${pageContext.request.contextPath}/deadlines/create">Create Deadline</a></c:if></section><c:if test="${not empty message}"><div class="alert alert-success"><c:out value="${message}" /></div></c:if><c:if test="${not empty error}"><div class="alert alert-warning"><c:out value="${error}" /></div></c:if>
<section><div class="feed-heading"><div><p class="eyebrow mb-1">Next up</p><h2 class="h3 mb-0">Upcoming deadlines</h2></div></div><div class="deadline-card-grid"><c:choose><c:when test="${empty upcomingDeadlines}"><div class="empty-state"><div class="empty-icon">D</div><h3>No upcoming deadlines.</h3><p>New academic deadlines will appear here.</p></div></c:when><c:otherwise><c:forEach var="deadline" items="${upcomingDeadlines}"><article class="deadline-card"><div class="deadline-card-top"><span class="deadline-status status-${deadline.status eq 'Due soon'?'soon':'upcoming'}"><c:out value="${deadline.status}" /></span><span class="deadline-scope"><c:out value="${deadline.scopeLabel}" /></span></div><h3><c:out value="${deadline.title}" /></h3><p class="deadline-subject"><c:out value="${deadline.subjectName}" /></p><time>Due <c:out value="${deadline.dueLabel}" /></time><p class="deadline-creator">Created by <c:out value="${deadline.creatorName}" /></p><c:if test="${not empty deadline.relatedPostTitle}"><p class="related-post">Announcement: <c:out value="${deadline.relatedPostTitle}" /></p></c:if><c:if test="${sessionScope.role eq 'ADMIN' or (sessionScope.role eq 'CR' and sessionScope.userId eq deadline.createdBy)}"><div class="deadline-actions"><a href="${pageContext.request.contextPath}/deadlines/edit?id=${deadline.deadlineId}">Edit</a><form method="post" action="${pageContext.request.contextPath}/deadlines/delete" onsubmit="return confirm('Are you sure you want to delete this deadline?');"><input type="hidden" name="csrfToken" value="<c:out value='${csrfToken}' />"><input type="hidden" name="id" value="${deadline.deadlineId}"><button type="submit">Delete</button></form></div></c:if></article></c:forEach></c:otherwise></c:choose></div></section>
<c:if test="${not empty pastDeadlines}"><section class="past-section"><div class="feed-heading"><div><p class="eyebrow mb-1">Archive</p><h2 class="h3 mb-0">Past deadlines</h2></div></div><div class="deadline-card-grid"><c:forEach var="deadline" items="${pastDeadlines}"><article class="deadline-card expired"><div class="deadline-card-top"><span class="deadline-status status-expired">Expired</span><span class="deadline-scope"><c:out value="${deadline.scopeLabel}" /></span></div><h3><c:out value="${deadline.title}" /></h3><p class="deadline-subject"><c:out value="${deadline.subjectName}" /></p><time>Was due <c:out value="${deadline.dueLabel}" /></time><c:if test="${sessionScope.role eq 'ADMIN' or (sessionScope.role eq 'CR' and sessionScope.userId eq deadline.createdBy)}"><div class="deadline-actions"><a href="${pageContext.request.contextPath}/deadlines/edit?id=${deadline.deadlineId}">Edit</a><form method="post" action="${pageContext.request.contextPath}/deadlines/delete" onsubmit="return confirm('Are you sure you want to delete this deadline?');"><input type="hidden" name="csrfToken" value="<c:out value='${csrfToken}' />"><input type="hidden" name="id" value="${deadline.deadlineId}"><button type="submit">Delete</button></form></div></c:if></article></c:forEach></div></section></c:if></main></div></body></html>
